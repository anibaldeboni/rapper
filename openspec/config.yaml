# OpenSpec configuration — rapper
# Auto-generated by sdd-init (2026-06-26)
context: |
  Go TUI application for batch HTTP requests driven by CSV data.
  Uses Bubbletea (Elm Architecture), Lipgloss, Bubbles v2.
  Module: github.com/anibaldeboni/rapper (Go 1.25).
  Dependency injection via interfaces + constructor functions.
  Mocks generated with go.uber.org/mock via //go:generate directives.
strict_tdd: true
test_command: go test -v -cover -race -timeout 30s ./...
test_framework: stretchr/testify + go.uber.org/mock
testing:
  test_runner:
    command: go test -v -cover -race -timeout 30s ./...
    framework: stretchr/testify
    mock_gen: go.uber.org/mock
  layers:
    unit: true
    integration: false
    e2e: false
  coverage:
    available: true
    command: go test -v -race -coverprofile=build/coverage.out -covermode=atomic -timeout 30s ./...
  quality:
    linter: golangci-lint (v2 config, copyloopvar, misspell, nilerr, perfsprint, prealloc, unconvert, unparam)
    formatter: gofmt + goimports (enforced via golangci-lint)
    type_checker: go vet (implicit, no dedicated config)
phases:
  proposal: enabled
  spec: enabled
  design: enabled
  tasks: enabled
  apply: enabled
  verify: enabled
  archive: enabled
